# =============================================================================
# OnBoardingRite Backend Environment Configuration
# =============================================================================
# INSTRUCTIONS FOR DEVOPS:
# 1. Copy this file to `.env` in the same directory
# 2. Replace all placeholder values with actual production values
# 3. Never commit the actual `.env` file to version control
# =============================================================================

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
APP_NAME=OnBoarding App
DEBUG=False

# -----------------------------------------------------------------------------
# PostgreSQL Database (REQUIRED)
# -----------------------------------------------------------------------------
# DevOps: Provide the PostgreSQL connection details
POSTGRES_HOST=<DATABASE_HOST>
POSTGRES_PORT=5432
POSTGRES_DB=<DATABASE_NAME>
POSTGRES_USER=<DATABASE_USER>
POSTGRES_PASSWORD=<DATABASE_PASSWORD>

# -----------------------------------------------------------------------------
# Security (REQUIRED)
# -----------------------------------------------------------------------------
# DevOps: Generate a strong random string (min 32 characters recommended)
# Example: openssl rand -hex 32
JWT_SECRET=<GENERATE_SECURE_RANDOM_STRING>

# -----------------------------------------------------------------------------
# CORS - Frontend Origins (REQUIRED for production)
# -----------------------------------------------------------------------------
# DevOps: Add your production frontend URL(s), comma-separated
# Example: https://onboarding.yourcompany.com,https://www.yourcompany.com
FRONTEND_ORIGINS=http://localhost:9009,http://localhost:5173,<PRODUCTION_FRONTEND_URL>
